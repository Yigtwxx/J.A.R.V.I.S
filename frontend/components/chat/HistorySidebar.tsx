// HistorySidebar has been superseded by the Command Center NavRail
// (left navigation rail + sliding panel drawer). Re-exported here so any
// existing import path keeps working.
export { default } from '@/components/chat/NavRail';
