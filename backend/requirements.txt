fastapi~=0.109.0
uvicorn[standard]~=0.27.0
sqlalchemy~=2.0.0
pydantic~=2.5.0
pydantic-settings~=2.1.0
python-dotenv~=1.0.0
httpx~=0.27.0
beautifulsoup4~=4.12.0
# lxml 6.x is required by scrapling (>=6.1.1); the old ~=5.1.0 pin made a clean
# `pip install -r requirements.txt` unresolvable. BeautifulSoup works with either.
lxml~=6.1
requests~=2.31.0
# Discovery pipeline: adaptive scraping (HTTP tier via curl_cffi, stealth tier
# via Camoufox). After install, run `scrapling install` once to fetch the
# browser — the pipeline degrades to the HTTP tier if it is missing.
scrapling[fetchers]~=0.4
tenacity~=9.0
ollama~=0.3.0
python-multipart~=0.0.6
rich~=13.0.0
deepface~=0.0.89
Pillow~=10.0.0
alembic~=1.13.0
fpdf2~=2.7.0
cachetools~=5.3.0
pytest~=8.0.0
pytest-asyncio~=0.23.0
redis~=5.0

# macOS only: the spatial console moves real windows through Quartz (to list
# them) and the Accessibility API (to place them). Windows needs nothing extra —
# it goes through ctypes — and neither does Linux, where window control reports
# itself unsupported rather than pretending to work.
pyobjc-framework-Quartz~=10.3; sys_platform == "darwin"
pyobjc-framework-ApplicationServices~=10.3; sys_platform == "darwin"
pyobjc-framework-Cocoa~=10.3; sys_platform == "darwin"
